/// API Configuration (TEMPLATE)
///
/// Copy this file to `api_config.dart` and fill in your own keys.
/// `api_config.dart` is git-ignored so real keys never get committed.
class ApiConfig {
  // Google Gemini API Key — https://aistudio.google.com/apikey
  static const String geminiApiKey = 'YOUR_GEMINI_API_KEY';

  // Image Generation API Key (Optional — null to use free services)
  // Get from Hugging Face / Replicate / Imagine AI.
  static const String? imageApiKey = 'YOUR_IMAGE_API_KEY';
}
